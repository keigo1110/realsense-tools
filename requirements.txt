numpy
opencv-python
matplotlib
pillow  # 3Dキーポイントアニメーション（GIF出力）用
toml  # 設定ファイル読み込み用

# YOLOv8-Pose用
# 高速で高精度な姿勢推定のため、YOLOv8-Poseを使用
# インストール: pip install ultralytics
ultralytics>=8.0.0

# ViTPose用（オプション、最高精度のキーポイント推定）
# インストール: pip install openmim
# その後: mim install mmengine mmcv mmdet mmpose
# 注意: mmposeのインストールには時間がかかる場合があります
# ViTPoseを使用しない場合は、このセクションをスキップできます

# Open3D: 2台のカメラの自動キャリブレーション用
# ターゲットレス自動外部標定（FPFH特徴量 + RANSAC + ICP）に使用
open3d>=0.18.0

# CUDA高速化用（オプション、CUDA環境の場合）
# CUDAが利用可能な環境では、pip install cupy-cuda11x または cupy-cuda12x をインストール
# CUDAバージョンに応じて適切なパッケージを選択してください
# 例: pip install cupy-cuda11x  # CUDA 11.x用
# 例: pip install cupy-cuda12x  # CUDA 12.x用
# CuPyがインストールされていない場合、自動的にNumPyにフォールバックします

# pyrealsense2: プラットフォームによって異なるパッケージを使用
# macOS: pyrealsense2-macosx または pyrealsense2-mac
# Linux/Windows: pyrealsense2 (公式パッケージ)
# 詳細はREADME.mdの「エラーが発生した場合」セクションを参照してください

# NorFair: 高精度な人物トラッキング用（オプション）
# より堅牢なトラッキングが必要な場合: pip install norfair
# 使用する場合: --use-norfair-tracking フラグを追加
